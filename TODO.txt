greeter
functional keys (mute, play, airplane mode, brightness)
bar mute red icon
fullscreen hide bar
plug monitor brightness control + auto-scale
cursor
qt arc
Rofi
termite
.gitconfig
xbanish

Vim
    system clipboard
    yankstack
    indent guides colorscheme
    xcape vim Esc
    Restore session all tabs
    reload tabs on branch switch

envtest bug

i3
    config
    colorscheme

Lock screen + xautolock
Android automount
.gitignore
    passwords SHAs
Git annex
Printers
Notifications?

Keybindings
    compose key
    rofi execute/open file
    i3
        xrandr
        redshift
        openterminal
        lock
        screen
    Termite
        vim mode
        copying
    .zshrc
        unbind ctrl moving
        oh-my-zsh lib/keybindings.sh
        autocompletion (arrow up, etc.)
    Tmux
        rebind new-pane split-pane so it opens in the current dir, not ~
    vim shortcuts
        !! vim-unimpaired !!
        rename var/method
        find usages
        go to def
        tabs
        yank history
        indent blocks
        text object selection
        fuzzy search out of home dir
        close(/save?) session

Add licenses
-----------------

After seting up the system:
    import GPG keys
    .gitignore gpgsign = true
    import SSH keys
    SSH aliases
